run-name: ci by ${{ github.actor }} - ${{ github.sha }}

on:
  push:
    branches:
      - 'main'

jobs:
  build_frontend:
    runs-on: ubuntu-latest
    steps:
      - run: echo "SHORT_SHA=`echo ${GITHUB_SHA} | cut -c1-8`" >> $GITHUB_ENV
      - run: echo ${SHORT_SHA}
      - run: echo meowalien.com:5000/frontend:${SHORT_SHA}
      - name: Check out repository code
        uses: actions/checkout@v3
      - run: git rev-parse --short HEAD
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v2
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      -
        name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          registry: meowalien.com:5000
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      -
        name: Build and push
        uses: docker/build-push-action@v3
        with:
          context: ${{ github.workspace }}/frontend/
          file: ${{ github.workspace }}/frontend/Dockerfile
          push: true
          tags: meowalien.com:5000/frontend:${SHORT_SHA}

#run-name: ${{ github.actor }} is building ${{ github.sha }}
#on: [push]
#jobs:
#  build:
##    https://docs.github.com/en/actions/using-github-hosted-runners/about-github-hosted-runners#supported-runners-and-hardware-resources
#    runs-on: ubuntu-latest
#    steps:
#      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
#      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
#      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
#      - name: Check out repository code
#        uses: actions/checkout@v3
#      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
#      - run: echo "🖥️ The workflow is now ready to test your code on the runner."
#      - name: List files in the repository
#        run: |
#          ls ${{ github.workspace }}
#      - run: echo "🍏 This job's status is ${{ job.status }}."